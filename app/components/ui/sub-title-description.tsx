import { type PropsWithChildren } from "react"

const SubTitleDescription = ({ children }: PropsWithChildren) => (
  <h5 className="font-medium text-foreground">{children}</h5>
)

export { SubTitleDescription }
